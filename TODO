persist gitea access token across restarts of the operator pod
checks to see if stuff exists when deployed (idempotency)
gitea in a unique namespace (or delete and recreate?)
bigbang-operator in its own namespace
